{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },  // Alternativ kannst du hier '/server.js' belassen
    { "src": "/(.*)", "dest": "/dist/index.html" }
  ]
}
