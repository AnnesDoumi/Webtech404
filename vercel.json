{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    },
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "redirects": [
    { "source": "/api/(.*)", "destination": "/server.js", "statusCode": 200 },
    { "source": "/(.*)", "destination": "/dist/index.html", "statusCode": 200 }
  ]
}
